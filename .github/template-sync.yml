# Copied into generated repos. Each repo pulls this template with GITHUB_TOKEN
# and opens a PR on itself. Nested templates keep extra keys via three_way.

# Parent to pull from. Empty = GitHub templateRepository of this repo.
source:
source_ref:
prefer_branch: dev

identity:
  extra:
    re.neotamia.kotlintemplate: re.{owner_slug}.{slug}
    neotamia-build: "{slug}-build"
  owner_files:
    - README.md
    - CONTRIBUTING.md
    - settings.gradle.kts
    - buildSrc/src/main/kotlin/*-build.gradle.kts

# Only the pull workflow itself: children must keep the same caller.
overwrite:
  - .github/workflows/template-sync.yml

# Shared keys from the template, dest-only keys (Paper, Discord, …) are kept.
merge_toml:
  - gradle/libs.versions.toml

# Apply the template delta, keep dest customizations. Conflicts stay in the PR.
three_way:
  - .editorconfig
  - .github/renovate.json5
  - .github/workflows/build.yml
  - .github/workflows/lint.yml
  - .github/workflows/test.yml
  - .github/workflows/release-please.yml
  - .github/ISSUE_TEMPLATE/**
  - .github/PULL_REQUEST_TEMPLATE.md
  - gradle/wrapper/**
  - buildSrc/build.gradle.kts
  - buildSrc/settings.gradle.kts
  - buildSrc/src/main/kotlin/*-build.gradle.kts
  - settings.gradle.kts
  - gradle.properties
  - CODE_OF_CONDUCT.md
  - CONTRIBUTING.md
  - AGENTS.md
  - .github/template-sync.yml

exclude:
  - .github/workflows/project-setup.yml
  - .github/template-sync-state.json
  - modules/**
  - README.md
