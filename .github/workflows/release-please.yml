name: Release Please Action

on:
  push:
    branches: [ dev ]
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  release-please:
    name: Release Please
    uses: NeoTamia/actions/.github/workflows/release-please.yml@e9f391cf23f73a5cab7fc8db5a3b28a6af0ae09e # main
    permissions:
      contents: write
      pull-requests: write
    secrets: inherit

  gitflow-release:
    name: Gitflow release
    needs: release-please
    if: ${{ needs.release-please.outputs.release_created == 'true' }}
    uses: NeoTamia/actions/.github/workflows/gitflow-release.yml@e9f391cf23f73a5cab7fc8db5a3b28a6af0ae09e # main
    permissions:
      contents: write
    secrets: inherit
    with:
      tag-name: ${{ needs.release-please.outputs.tag_name }}
      version: ${{ needs.release-please.outputs.version }}
